import { InfoProducts } from "@/components/PageFrames/InfoProducts";
import { NextPage } from "next";
import { Fragment } from "react";

const ProductPage: NextPage = () => {
  return (
    <Fragment>
      <InfoProducts />
    </Fragment>
  );
};

export default ProductPage;
